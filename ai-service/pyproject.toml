[tool.pytest.ini_options]
markers = ["model: loads the real sentence-transformers model (slow)"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
